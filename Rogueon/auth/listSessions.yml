info:
  name: listSessions
  type: http
  seq: 6

http:
  method: GET
  url: "{{authBaseUrl}}/list-sessions"
  headers:
    - name: Origin
      value: http://localhost:5173
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
