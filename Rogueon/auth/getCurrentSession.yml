info:
  name: getCurrentSession
  type: http
  seq: 4

http:
  method: GET
  url: "{{authBaseUrl}}/get-session"
  headers:
    - name: Origin
      value: http://localhost:5173
  auth: inherit

settings:
  encodeUrl: true
  timeout: 0
  followRedirects: true
  maxRedirects: 5
